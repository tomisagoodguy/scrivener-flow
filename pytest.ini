[pytest]
minversion = 7.0
addopts = -ra -q --strict-markers
testpaths = tests
pythonpath = .

# 測試標記
markers =
    integration: 整合測試，需要外部服務 (deselect with '-m "not integration"')
    slow: 耗時測試 (deselect with '-m "not slow"')

# 忽略特定警告
filterwarnings =
    ignore::DeprecationWarning
